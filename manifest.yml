# Generated manifest.yml based on template version 0.1.0
# appName = ssm
# language=java
# multitenancy=false
---
applications:
  # -----------------------------------------------------------------------------------
  # Backend Service
  # -----------------------------------------------------------------------------------
  - name: ssm-srv
    random-route: true # for development only
    path: srv/target/ssm-exec.jar
    memory: 1024M
    disk_quota: 1024M
    instances: 1
    env:
      SPRING_PROFILES_ACTIVE: cloud
    buildpack: java_buildpack
